const initialState = {
  passengers: [],
  loading: true,
  errorMessage: '',
}

// const GET_PASSENGERS = 'GET_PASSENGERS';
// const GET_PASSENGERS_SUCCESS = 'GET_PASSENGERS_SUCCESS';
// const GET_PASSENGERS_REJECTED = 'GET_PASSENGERS_REJECTED';

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
